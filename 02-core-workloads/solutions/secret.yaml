# Exercise 2.3 — a Secret. stringData lets you write plain text; k8s encodes it.
# NOTE: base64 is NOT encryption. Never commit real secrets to Git.
apiVersion: v1
kind: Secret
metadata:
  name: hello-secret
type: Opaque
stringData:
  APP_ENV: production
  API_KEY: super-secret-value
